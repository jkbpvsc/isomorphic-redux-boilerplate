// Imports
import React from 'react'

// App Imports
import Index from '../components/home/Index'

// Routes
const routes = [
    {
        path: '/',
        component: Index,
        exact: true
    }
];

export default routes